# PostgreSQL 数据库名/用户/密码（必填）
POSTGRES_DB=app_web
POSTGRES_USER=fsdx
POSTGRES_PASSWORD=change-me-in-production-strong-password

# JWT access token 密钥（务必修改为强随机字符串，至少 32 位）
JWT_SECRET=change-me-in-production-at-least-32-characters-long

# 日志级别：fatal | error | warn | info | debug | trace
LOG_LEVEL=info

# 系统配置敏感值加密主密钥（启用敏感配置后必填）
# 32 字节的 Base64 或 64 位十六进制字符串（openssl rand -base64 32）
# 密钥丢失后已加密的敏感配置无法解密恢复，请妥善备份
# CONFIG_ENCRYPTION_KEY=

# 宿主映射端口（可选）
APP_PORT=3000
DB_PORT=14010
